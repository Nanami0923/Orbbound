# Snood 现代化重构方案

日期：2026-09-11  
状态：产品与技术立项方案，未进入编码阶段  
参考对象：[Snood (The Original)](https://www.snoodworld.com/snood-game/snood/)

## 1. 推荐结论

建议以 **“保留策略消除核心，重做视觉与操作，优先发布跨设备网页版本”** 为方向。先做不依赖账号和服务器的完整单机 MVP，验证瞄准手感、消除反馈与关卡质量，再增加每日挑战、云存档和排行榜。

推荐组合：**TypeScript + Phaser + Vite，规则引擎独立于画面，关卡数据驱动**。第一版不做实时对战、复杂道具或重度养成，避免把轻量益智游戏变成大型服务项目。

在没有原始代码的前提下，本方案属于基于玩法规格的重新实现；获得源码后，再评估哪些逻辑和资源值得迁移。工期假设为一名熟悉网页游戏的开发者，配合兼职美术及测试，约 6–8 周完成可公开试玩的版本。

## 2. 调研依据与边界

用户提供的链接是原版产品介绍与下载页，不是可直接试玩的游戏画布。此次核实了官网文字说明，未运行原版客户端、审查源码或完成逐帧操作对照，因此不对原版动画质量、触控体验或内部算法作事实判断。

官网明确说明：连接三个及以上同类角色可以消除；失去顶部连接的角色会掉落；发射会增加危险程度，危险条满后棋盘下降，角色触底则结束。[原版介绍](https://www.snoodworld.com/snood-game/snood/)、[官网玩法概述](https://www.snoodworld.com/)

以下内容是需要通过试玩进一步核对的细节：墙面反弹、精确吸附方式、计分公式、危险条增减量、下降距离、颜色生成策略和各模式差异。本文为这些细节提供可实现的新版本默认方案，不把它们当作原版事实。

页面还列出了账号、每日挑战和成绩比较，因此这些并不是本方案宣称发明的新功能；现代化重点应是更低的进入门槛、更清楚的反馈和可靠的跨设备体验。[原版介绍](https://www.snoodworld.com/snood-game/snood/)

## 3. 产品定位与范围

### 3.1 用户体验目标

- 打开链接即可开始，不要求注册或安装。
- 新玩家通过三个可操作的小步骤理解“匹配、断开支撑、管理危险条”。
- 老玩家能关闭辅助线和部分特效，快速进入连续对局。
- 鼠标、触屏与键盘都可以完成一整局。
- 主要体验是观察、选择角度和创造大面积掉落，不强制要求快速反应。

### 3.2 功能优先级

| 优先级 | 内容 | 完成标准 |
| --- | --- | --- |
| P0：首版必需 | 发射、碰撞、吸附、匹配、悬空掉落、危险条、胜负 | 从开始到结算形成完整闭环 |
| P0 | 一个经典规则集、三个难度预设 | 难度通过颜色数、初始布局与危险速度调整 |
| P0 | 触屏与鼠标操作、键盘基础支持 | 无误发射，操作方式可发现 |
| P0 | 教程、暂停、重新开始、音量、辅助显示 | 可独立完成教学并修改设置 |
| P0 | 本地续局、最高分、设置保存 | 刷新后从最近一次完整回合恢复 |
| P0 | 一套完整美术与音效、响应式布局 | 手机竖屏与桌面均可正常游玩 |
| P1：验证后加入 | 精选解谜关卡、每日固定种子挑战 | 规则与关卡版本固定，结果可复现 |
| P1 | 离线缓存、安装入口、更多主题 | 联网加载过的必要资源可离线使用 |
| P2：需要服务端 | 账号、云存档、在线排名 | 服务端验证结果，支持存档冲突处理 |
| 暂不开发 | 实时对战、公会、抽卡、复杂经济系统 | 不计入本次预算与排期 |

## 4. 视觉与交互设计

### 4.1 视觉方向：有表情的轻质玩具

角色保留“能辨认的表情”这一体验方向，采用圆润轮廓、清晰描边、轻微立体阴影。若没有原作素材授权，则使用原创名称、角色和声音。

建议用深靛蓝背景 `#141B2D`、暖白文字 `#F5F2EB`，配珊瑚、青绿、金黄等角色色。背景低对比，棋盘始终最醒目；不叠加复杂景深、大片发光或影响判断的装饰。

每种角色同时使用独特颜色、内部符号和表情，不能只靠颜色区分。视觉验收应包含灰度检查、小尺寸检查和实际背景下的可读性检查。

### 4.2 页面布局

首页只突出“开始游戏”，下方放继续游戏、玩法说明和设置。模式较多后再增加模式选择页，首版不做复杂大厅。

手机竖屏示意：

```text
┌──────────────────────────┐
│ 暂停    分数 01250    音效 │
│ 危险程度  █████░░░       │
├──────────────────────────┤
│      顶部固定边界         │
│    ◉ ◉ ◉ ◉ ◉ ◉         │
│     ◉ ◉ ◉ ◉ ◉          │
│                          │
│         瞄准路径         │
│            ·             │
│            ↑             │
│ 底线 ────────────────── │
│ 下一枚 ◉   发射器 ◉      │
└──────────────────────────┘
```

桌面保留相同棋盘比例，将统计和帮助放在侧边。宽屏不会获得更大的有效棋盘，避免设备不同导致难度变化。适配时缩放画面，不改逻辑坐标和网格。

### 4.3 操作规则

| 设备 | 默认操作 | 防误触设计 |
| --- | --- | --- |
| 鼠标 | 移动瞄准，点击棋盘发射 | 点击菜单不触发游戏；结算期间锁定发射 |
| 触屏 | 按住拖动瞄准，松开发射 | 出现明确预览后才允许发射；拖到取消区取消 |
| 键盘 | 左右调整角度，空格发射，Esc 暂停 | 支持持续微调与可见焦点，暂停后不透传输入 |

瞄准辅助默认显示第一段路径和最多一次反弹，并标出预期落点。辅助必须与实际碰撞共用算法；无法精确预测时减少提示范围，不能画出误导路径。竞技模式若限制辅助，应使用独立规则和榜单。

移动端提供可选“独立发射按钮”模式，供不适应松手发射的用户使用。按钮建议至少 44×44 CSS 像素，避开系统底部手势区域。

### 4.4 动效与反馈

- 发射：短促声音、轻微炮口压缩，输入立即得到反馈。
- 匹配：约 120–180ms 的缩放与消失，分数就近浮现。
- 大面积掉落：约 250–400ms 的分批坠落，强化“切断支撑”的成就感。
- 危险临界：同时显示文字与条形变化，不只闪红。
- 失败：指出触底区域，提供明显的重试按钮。
- 减少动态效果模式：关闭震屏和大幅弹跳，保留必要状态反馈。

以上时间为首轮调参起点，需通过试玩修正；动画不能改变规则结算结果。

## 5. 玩法实现规格

以下为新版本建议规格。原版精确复刻需要另行进行客户端对照。

### 5.1 单次发射的结算顺序

1. 接收瞄准角度，生成一枚飞行角色；同一时间只允许一个有效发射。
2. 检测其与侧墙、顶部及棋盘角色的首次接触，处理反弹或吸附。
3. 吸附到合法空格；不能覆盖已有角色或穿越阻挡格。
4. 从落点搜索同类连通区域，数量达到 3 时移除。
5. 从顶部所有剩余角色出发搜索连接，移除未连接到顶部的角色。
6. 计算得分、增加本次发射的危险值，必要时下移棋盘。
7. 检查清空胜利或触底失败；未结束时生成下一枚并保存回合状态。

危险结算顺序必须固定：建议先消除和掉落，再下降和判负。清空棋盘直接胜利，不再触发无意义的下降。避免玩家已经完成拯救却因动画顺序被判失败。

### 5.2 网格与碰撞

采用错行六邻接网格。设角色直径为 `d`，格点横向间距为 `d`，纵向间距为 `sqrt(3)/2 × d`，隔行横向偏移 `d/2`。这只是建议实现，不代表已经确认原作内部结构。

飞行阶段使用圆形碰撞；落盘后全部逻辑改为网格运算。移动碰撞使用扫掠检测或确定性细分步进，避免低帧率时高速穿透。

吸附在接触位置附近的合法邻格中选择，按距离排序，同距离时使用固定格点顺序。无合法邻格时触发明确的溢出失败，不把角色传送到远处。反弹、预测与实射共用几何模块。

匹配搜索和顶部连通搜索均可用 BFS/DFS。每回合扫描规模为棋盘格子数量的量级，此类小棋盘不需要复杂物理引擎。

### 5.3 初始数值建议

| 参数 | 首轮建议 | 调整目的 |
| --- | --- | --- |
| 普通难度 | 5 类角色、约 10 列、6–8 行初始布局 | 保持局面可读，提供断连机会 |
| 匹配阈值 | 3 枚 | 保留基础识别方式 |
| 危险条 | 上限 100，每次发射 +10，满后下降一行高度并扣除 100 | 让玩家理解发射成本 |
| 计分 | 匹配每枚 10 分，悬空掉落每枚 20 分 | 鼓励断连策略 |
| 颜色生成 | 从当前棋盘仍存在的颜色集合中选择 | 避免发射无法匹配的孤立颜色 |
| 下一枚 | 显示一枚预览，首版不提供交换 | 降低学习负担，避免额外平衡问题 |

这些是测试参数，不是原作公式，也不是经过验证的最佳平衡。若预览颜色在当前回合消失，需按固定规则刷新预览，刷新过程也应使用同一随机种子。

经典模式默认不随等待时间增加危险。解谜模式可在后续使用固定布局、固定发射序列和步数限制；随机布局不宣称必定可解，精选解谜关卡必须保存至少一条有效解法。

## 6. 技术方案

### 6.1 选型比较

| 路线 | 优点 | 代价 | 结论 |
| --- | --- | --- | --- |
| TypeScript + Phaser | 面向 2D 网页游戏，适合场景、输入、动画和资源管理 | 需要规范游戏与页面的职责边界 | 本项目推荐 |
| TypeScript + 原生 Canvas | 依赖少，底层控制直接 | 资源、音频、缩放和场景管理需要自行实现 | 适合教学原型 |
| 原生应用导向的游戏引擎 | 适合后续复杂原生游戏需求 | 对本次网页首发增加工具链与分发验证工作 | 原生优先时重新评估 |

Phaser 官方资料提供 TypeScript 开发说明，并描述了 WebGL/Canvas 渲染支持，适合本项目的浏览器方向。[官方文档](https://docs.phaser.io/)、[官方仓库](https://github.com/phaserjs/phaser)

工程初始化时固定具体 Phaser 版本和依赖锁文件，并验证对应版本的渲染、移动端输入与示例兼容性；不要混用不同主版本教程。Vite 用于开发和打包。首版外围页面用简单 HTML/CSS 即可；仅当大厅、账号、内容页变复杂时引入 React，避免每帧游戏状态驱动页面组件重渲染。

### 6.2 模块边界

```text
src/
  core/        网格、碰撞、消除、危险条、计分、随机数
  game/        场景、输入映射、角色渲染、动画、音效
  ui/          首页、设置、教程、暂停、结算
  content/     关卡和难度配置、资源清单
  storage/     本地存档、版本迁移
  services/    后续每日挑战和排行榜接口
```

`core` 不依赖浏览器或 Phaser。输入为“当前状态 + 发射命令”，输出为“新状态 + 事件列表”。画面监听发射、匹配、掉落、下降等事件播放动画，动画结束后开放下一次输入。

状态机建议为：`READY → AIMING → FLYING → RESOLVING → READY / WON / LOST`。暂停作为可恢复的外围状态处理。页面隐藏时自动暂停，恢复时重置帧时间，避免后台停留导致飞行跳跃。

### 6.3 存档与确定性

存档包含 `schemaVersion`、`rulesVersion`、关卡标识、棋盘、分数、危险值、当前及下一枚角色、随机数状态和已用步数。

只在完整回合结束后提交存档，刷新时恢复上一完整回合。小体量设置与续局可使用 localStorage；回放和大量关卡进度增加后再使用 IndexedDB。读档必须校验版本和数据结构，损坏时保留设置并提示重新开局。

随机数由显式种子和内部状态驱动。回放记录规则版本、关卡版本、初始种子和量化后的发射角度。跨设备复现还需要固定步进、固定碰撞排序及数值精度约定；仅记录种子不足以保证一致。

### 6.4 部署与联网边界

MVP 输出静态网页资源，通过 HTTPS 静态托管发布，暂不需要业务服务器。音频在用户首次交互后启动，并允许静音。

P1 增加离线缓存时，采用带版本的资源清单；更新在对局结束后切换，避免一局中混用新旧规则或素材。

在线排名放到 P2：服务器下发挑战标识、种子和规则版本，客户端提交发射记录，服务器重放计算分数。不能直接信任客户端上传的最终成绩。每日挑战明确刷新时区；重复提交、异常输入和榜单规则升级必须有统一处理。

## 7. 开发安排与交付物

以下是估算，不是已验证进度承诺。前提是已有网页开发经验、兼职美术可及时交付，且首版不接支付、账号或在线排名。

| 阶段 | 时间 | 交付物与阶段门槛 |
| --- | --- | --- |
| 规则与风险验证 | 第 1 周 | 规则表、灰盒棋盘、碰撞与吸附原型；先证明反弹和落点可靠 |
| 完整游戏闭环 | 第 2–3 周 | 可从开始玩到胜负的版本；消除、掉落、危险条与续局通过检查 |
| 视觉与多端体验 | 第 4–5 周 | 完整角色、音效、动效、手机操作、教程与设置 |
| 试玩与修正 | 第 6 周 | 小规模玩家观察、数值调整、真机兼容与性能报告 |
| 发布缓冲 | 第 7–8 周 | 修复遗留问题、资源压缩、部署说明、公开试玩构建 |

投入估算：开发 30–40 人日，美术与声音 8–12 人日，测试与试玩整理 5–8 人日；部分工作可以交叠。费用按各角色人日单价计算，另加约 15%–20% 风险缓冲，不在缺少团队与采购信息时给出虚假精确报价。

若由一人兼任开发、美术和测试，建议按 8–12 周规划；若必须 2–3 周交付，只交付一个难度、占位美术和核心闭环的原型，不将其定义为正式版。

## 8. 验收标准

### 8.1 规则与稳定性

- 用固定局面验证：恰好 3 枚消除、2 枚不消除、多分支同色连通和大面积悬空掉落。
- 用边界局面验证：墙角、连续反弹、顶部吸附、已满邻格、危险条临界、最后一枚消除与触底竞争。
- 保证无重叠占格、无越界格点、无结算期间重复发射；辅助落点与实际落点一致。
- 同版本、同种子、同输入序列，在目标设备上得到相同棋盘与分数。
- 暂停、切后台、旋转、刷新和读档后，不出现双重计分或额外发射。

### 8.2 性能与可用性目标

以下为验收目标，须在第 1 周选定设备清单后实测，不代表当前已经达到。

| 项目 | 目标与测量方式 |
| --- | --- |
| 加载 | 首屏必需压缩资源争取 ≤3MB，音乐延后加载；在约定的 10Mbps/100ms 网络条件下冷启动 ≤5 秒 |
| 流畅度 | 选定中档手机常规场景目标 60fps；大掉落场景持续帧率不低于 30fps |
| 操作反馈 | 从输入到出现瞄准反馈目标 ≤100ms，真机观察并用性能记录确认 |
| 兼容性 | Windows Chrome/Edge、macOS Safari、iOS Safari、Android Chrome，记录具体系统与浏览器版本 |
| 屏幕适配 | 360px 宽手机和桌面均无关键控件遮挡；缩放后触点位置准确 |
| 教学 | 招募 5–8 名首次接触者，至少 80% 无口头帮助完成教学并说清危险条规则 |

基础无障碍范围包括键盘操作、符号辅助辨色、文本状态提示、独立音量与减少动态效果。Canvas 棋盘若要完整支持屏幕阅读器，需要另做语义化棋盘导航，不能仅因菜单可读就宣称完整无障碍支持。

## 9. 主要风险与应对

| 风险 | 处理方式 |
| --- | --- |
| 没有源码，无法原样复刻手感 | 先按明确规格重写；需要忠实复刻时增加客户端试玩和行为对照阶段 |
| 美术更新后角色难以区分 | 先验证六种角色的小尺寸与灰度识别，再扩展完整动画 |
| 预览准确但实际落点错误 | 共用几何算法，优先解决此问题，再增加视觉特效 |
| 随机生成出现劣质局面 | 控制布局与颜色集合，收集种子复现；精选关卡保存解法 |
| 触屏手指遮挡 | 瞄准线向上延伸，提供独立发射按钮设置，并进行真机试玩 |
| 动效拖慢游戏节奏 | 为反馈设置时长预算，低性能档减少粒子与震屏 |
| 排行榜带来额外维护 | 首版只做本地成绩，联网阶段再实现服务端重放 |

名称与素材的项目边界：官网标明 Snood 为注册商标。未提供素材授权时，交付采用原创品牌与资产；若属于获授权重制，再将原作资源纳入资产清单。[官网](https://www.snoodworld.com/)

## 10. 建议立项基线

按以下默认条件即可开始第一阶段：**网页首发、手机竖屏优先、原创角色、一套经典规则、三个难度、本地存档、无账号、无服务端**。第一个可评审成果是灰盒可玩原型，重点确认“瞄得准、落得对、消除与掉落有策略价值”。

进入开发前需要锁定的仅是会明显改变工作量的条件：是否拥有原作源码或素材授权、是否要求忠实复刻具体版本、是否必须首发原生应用，以及可投入的人力与上线日期。没有额外要求时，沿用本文基线即可。

## 参考资料

资料访问日期：2026-09-11。玩法事实来自 Snood 官方说明；产品改造、默认参数、架构和工期为本方案建议。

1. [Snood (The Original)：原版规则与产品功能](https://www.snoodworld.com/snood-game/snood/)
2. [Snood World：顶部连通与掉落玩法说明](https://www.snoodworld.com/)
3. [Phaser 官方文档：框架能力与开发入口](https://docs.phaser.io/)
4. [Phaser 官方仓库：源码、类型与版本信息](https://github.com/phaserjs/phaser)


# 3.2.0 验证记录

日期：2026-09-14。Windows 与 Android 均为 3.2.0，Android versionCode 30200。测试截图、测试存档、临时诊断工具与环境均不发布。

## 自动测试

18 个测试文件、106 项测试通过，类型检查通过。新增覆盖：快捷键冲突与非法按键、重启保存和备份兼容、组合键与输入法保护、沉浸模式发射区瞄准、释放只发射一次、多指与越界、取消与生命周期复位、飞行中不排队补发、触摸默认行为处理，以及异步音频恢复立即调度。原有规则、计分、输入和音频淡出回归仍通过。

## Windows 正式目录

- 鼠标左键、右键和空格分别成功发射，右键不打开上下文菜单。
- 左侧发射按钮、下一球卡和存档按钮已移除；右侧显示下一球及统一选项。存档与返回合并。
- 在设置中把发射改为 X、存档改为 V：重复绑定 R 被拒绝；界面提示同步变化，原空格/Esc 不再触发旧动作，新按键正常工作；重启后保存。
- 默认 S 打开设置，H 打开记录，I 打开玩法提示；提示使用实际的自定义发射键。
- 1280×800、840×540、640×400、520×310 CSS 像素布局检查通过，棋盘与左右栏未越出视口。
- 在没有额外点击的情况下，实际 WebView2 音频上下文为 running；创建后约 2.2 ms 调度首个音符，进入游戏后继续运行，始终只有一个游戏音频上下文。该值是本机调度测量，不是扬声器端声学延迟；没有进行系统音频回录。
- 页面没有捕获到 JavaScript 异常。仍采用共享 WebView2 轻量外壳，不随游戏上传运行环境。

## 手机与触摸

- Xiaomi 2210132C / Android 16 真机覆盖安装正式签名 APK，原数据保留。沉浸设置重启与覆盖安装后保持。
- 开启后底部双滑条和发射键隐藏，棋盘下移；从发射区域按住拖动可改变炮口方向，约 1.6 秒拖动后松手成功发射一次。
- 浏览器触摸自动化验证按住期间不发射、松手一次、系统取消不发射，关闭沉浸后恢复按钮；393×873、360×640、320×568 与 873×393 横屏检查通过。修正引擎内联尺寸覆盖 CSS 所导致的棋盘拉伸，保持 640:760 比例。
- 实测发现本机系统会在原地静止长按约两秒后触发图片识别并发送取消事件。按住后移动可继续长按操作；按用户确认，本版保留该系统行为，不更改手机全局设置。
- 临时 WebView 调试开关已从正式源码和最终构建移除。沿用原签名，发布仅 APK、Windows ZIP 和各自 SHA-256 校验文件。

真机测试只覆盖上述一部手机，不能代表所有厂商的系统手势行为。Android 图片识别属于已确认保留的边界，不计为“已修复”。

# Orbbound

## Android 2.5.0

增强音量与音乐响度、旋转炮口、边瞄准边发射、界面与轨迹计算优化。[完整说明](Android/android-2.5.0-notes.md)。


## Android 2.4.0

双滑条操作、递增消除与悬空奖励、音量与本地循环 BGM、辅助线优化。[完整说明](Android/android-2.4.0-notes.md)。


## Android 2.3.0

- 结算图标按卡片中心对齐，关闭按钮独立定位；提示按语义和标点分为两行。
- 左转、右转与发射统一图标及文字布局；左右下方新增 30° 快转键，260ms 平滑过渡，连续点按可累加目标，角度仍限制在 ±78°。
- 下落及飞行期间可继续转向，长按加速不中断；动画期间显示方向短线，落定后恢复辅助线，发射仍等待动画完成。松手、换向、切后台及存档会停止转向，避免恢复后误转。
- 沿用 2.2.0 的双倒计时、难度、计分和独立存档规则。

下载：[Android 2.3.0 APK](https://github.com/Nanami0923/Orbbound/releases/download/v2.3.0/Orbbound-2.3.0-Android.apk)。输出目录：`最终交付/Orbbound-2.3.0/`。versionCode 20300，沿用原签名，可覆盖安装保留本地数据。

验证：57 项自动测试通过，覆盖两模式连续转向、快转过渡及边界、操作中断、下落期间发射锁定，以及原有倒计时、存档和计分回归。未连接实体手机，未做真机安装及帧率基准测试。

## Android 2.2.0

- 限时下落提示拆为两条独立倒计时：绿色次数条按剩余发射次数减少，金色时间条按本次下落剩余时间缩短；时间不足 5 秒时变红。右侧继续独立显示整局剩余时间。任意一项归零下降后，两条同时复位，存档时冻结。
- 时间条按本次下落周期的固定总时长计算进度，避免难度随进度变化造成比例跳动；每 100ms 更新一次，使用变换平滑显示，并避免重复重写未变化的文字和小球预览。
- 无尽 / 限时统一使用平滑长按加速：保留点按 1° 微调，长按由 12°/秒平滑提高至 96°/秒，约 1.8 秒达到上限；起步和到达上限时加速度平滑衔接。松手、换向、发射及存档时重置，不产生惯性漂移，仍限制在 ±78°。
- 三档限时下落间隔适当延长：简单 36→18 秒、普通 30→15 秒、困难 24→12 秒。仍随本局有效游玩进度线性缩短，5 / 10 分钟时长、发射次数阈值和计分系数不变。
- 保留独立存档及榜单逻辑。旧存档当前剩余时间继续沿用，下一次下落后应用新版间隔。

下载：[Android 2.2.0 APK](https://github.com/Nanami0923/Orbbound/releases/download/v2.2.0/Orbbound-2.2.0-Android.apk)。输出目录：`最终交付/Orbbound-2.2.0/`。沿用原包名及发行签名，versionCode 20200，直接覆盖安装保留本地数据。

验证：53 项自动测试通过，覆盖独立倒计时比例、双条复位与存档恢复、新难度间隔、两模式 30/60/120 FPS 转向一致性、平滑加速上限和输入锁定；生产构建、320×568 / 390×844 手机浏览器界面与交互、APK 签名校验。未连接实体手机，未做真机安装及帧率基准测试。

## Android 2.1.0

- 限时下落的次数 / 时间任意一项先归零，下降一行后同时重置两项；修复简单、困难按次数下降后残留计数余量的问题。随本局有效游玩时间缩短下落间隔的逻辑不变：简单 30→14 秒、普通 24→11 秒、困难 18→8 秒。
- 手机顶部不再显示重复的下一球预览。保留炮口右侧提示，大小由 0.72 倍放大至 1.35 倍，并标注「下一球」；关闭辅助线时仍然显示。
- 排行榜 / 历史记录改为点击标题切换，标题旁显示切换图标，移除原来占据一行的两个切换按钮；切换时保留所选模式榜单。
- 新增限时单局独立存档，首页及模式设置均可读取。无尽、限时各保留一局，两种存档互不覆盖；限时的 5 / 10 分钟共用该限时存档位，新开同模式局会替换该模式的旧存档。
- 两种模式存档、打开菜单和切后台均冻结进度；限时读取后继续剩余整局时间、下落时间及剩余发射次数，离线时间不再扣除。2.0 的未完成限时记录按最后保存的有效游玩进度迁移为可读取存档。
- 临时存档不计入排行榜或已结算历史；仅到时、触底、清盘或主动结算后入榜。重开仍只保留不入榜的放弃记录。保存恢复保留原会话标识，防止一局重复计分。

下载：[Android 2.1.0 APK](https://github.com/Nanami0923/Orbbound/releases/download/v2.1.0/Orbbound-2.1.0-Android.apk)。输出目录：`最终交付/Orbbound-2.1.0/`。沿用原包名和发行签名，versionCode 20100，直接覆盖安装保留本地数据。

验证：47 项自动测试通过，覆盖三档难度双计数重置、冻结 / 续玩、旧限时存档迁移、临时存档不入榜和辅助线关闭时的下一球显示；生产构建、手机浏览器 320×568 / 390×844 布局与交互、APK 签名校验。未连接实体安卓手机，未进行真机安装或帧率基准测试。本次仅发布 Android APK。

## Android 2.0.0

- 首页新增「无尽模式」「限时模式」两个入口，模式设置中选择简单、普通、困难；手机单屏布局保留底部握持操作区。
- 限时提供 5 分钟、10 分钟，分别使用独立排行榜；无尽排行榜也独立保留前十。原始分按简单 ×1、普通 ×1.5、困难 ×2 加权后排名，同分时用时短者优先。
- 限时下落遵循「发射次数或时间先到即下落」。简单间隔从 30 秒线性缩短到 14 秒，普通 24→11 秒，困难 18→8 秒；按整局进度计算，次数提前触发下落时重置下落计时。强制下落重置发射计数。
- 限时不可暂停，菜单、切后台和关闭应用均不停表；重新打开按原截止时间补算下落，可能触底。清盘补充新棋盘继续挑战，时间到或触底结算，也可主动结束计分。
- 无尽保留原有清盘 / 触底规则，暂停入口改为「存档并返回首页」。保存棋盘、球序列、随机状态、发射次数、分数及用时，关闭应用后仍可读取，继续局与主动结算均参与排名。新开无尽局会替换原存档，重开不计入排行榜。
- 两种模式的本地状态分开保存；旧存档可继续，已有可读取成绩一次性按难度系数迁移。历史保留最近 500 局，各榜前十独立保存。卸载或清除应用数据会删除本地记录。
- 发射时提交完整规则状态，防止动画中退出丢失分数或重复发射。计时显示每秒刷新，复用棋盘对象和共享纹理，局内存档不再反复读取首页排行榜。

下载：[Android 2.0.0 APK](https://github.com/Nanami0923/Orbbound/releases/download/v2.0.0/Orbbound-2.0.0-Android.apk)。本地文件：`最终交付/Orbbound-2.0.0/Orbbound-2.0.0-Android.apk`，附 SHA-256 文件。包名和签名沿用旧版，versionCode 为 20000，可覆盖安装。

验证：36 项自动测试通过，覆盖两模式存档隔离、旧成绩迁移、三个榜单、后台补算、到时中断飞行动画与重复结算保护；生产构建和 APK 签名校验通过，320×568 / 390×844 手机浏览器检查通过。未连接实体安卓手机，未做真机安装或帧率基准测试。

Windows 已发布的 1.3.0 安装包与标签保持不变；本次仅发布 Android APK。

## Windows 1.3.0（独立版本）

- 高清画布 1280×1520、小球纹理 192×192；移除外投影，描边限制在球体内，避免相邻小球遮挡。
- 保留桌面左右信息栏和居中棋盘，鼠标瞄准、点击发射及键盘微调。
- 复用棋盘小球对象、合并消除动画、飞行位置连续插值，减少对象重建和跳动。
- 无辅助线炮口由旧 Windows 版的 36 延长到 84 游戏坐标单位。
- 桌面打包使用独立最小清单，排除 Android 运行依赖和开发文件；只保留中英文运行语言，使用 7z 固实压缩减小下载体积。

游戏目录：`最终交付/Orbbound-Windows-1.3.0/win-unpacked`，双击其中的 `Orbbound.exe`。下载压缩包后用 7-Zip 解压整个目录。

[Windows 1.3.0 下载](https://github.com/Nanami0923/Orbbound/releases/tag/windows-v1.3.0)

Windows 版本由 `electron/windows-package.json` 管理，发布标签使用 `windows-v*`；Android 版本仍为 1.7.0，原 APK 与标签保持不变。桌面存档继续使用 `%APPDATA%/orbbound`。

验证：24 项自动测试通过；1280×800 和 900×540 桌面视口、鼠标发射结算检查通过；构建目录启动检查通过。未做不同配置电脑的帧率基准测试。

## 1.6.0 按钮、炮口与动画优化

- 手机三个操作按钮高度由 64 增至 84 CSS 像素，保留中下部握持位置。
- 无辅助线模式炮口指示从 36 延长至 84 游戏坐标单位，仍不显示完整轨迹。
- 棋盘更新复用未变化的小球，仅创建或销毁变化的格子；同延迟消除合并为一组动画，减少动画管理开销。
- 飞行位置在轨迹采样点之间连续插值，减少跳动；结算等待与实际动画时长匹配，去掉无意义的临时文字创建。
- 24 项自动测试通过，含对象复用、炮口长度及下降动画输入锁定；浏览器手机尺寸检查通过。未进行实体手机性能测量。

下载：[1.6.0 APK](https://github.com/Nanami0923/Orbbound/releases/download/v1.6.0/Orbbound-1.6.0-Android.apk)。沿用原签名和包名，可直接覆盖安装，versionCode 10600。

## 1.5.0 握持布局与平滑转向

- 首页去掉大块空白面板，上方标题与球体重新排布；操作区集中在屏幕中下部，上下保留握持留白。
- 顶部菜单和提示保持位置，棋盘整体上移并保持 640:760 比例；长屏操作区中心约在屏幕高度 69% 处，小屏自适应缩放。
- 转向由 40ms 定时跳动改为逐帧时间积分：按下立即微调 1°，初速 18°/秒，加速度 60°/秒²，最高 90°/秒；松手、暂停、发射时停止，换向重新加速。
- 辅助线落点计算复用本次棋盘坐标，减少重复遍历；本机 500 组轨迹与 1.4.0 完全一致，单次基准耗时约从 81ms 降至 58ms（非手机帧率承诺）。
- 22 项自动测试通过，覆盖 30/60/120 FPS 转角一致、松手停止、换向、暂停与角度限制；检查 320×568 和 412×915 界面。未进行实体手机安装及性能实测。

下载：[1.5.0 APK](https://github.com/Nanami0923/Orbbound/releases/download/v1.5.0/Orbbound-1.5.0-Android.apk)。沿用原包名和签名，可覆盖安装；versionCode 为 10500。

## 1.4.0 手机操作更新

- 首页与局内改为固定单屏，禁止页面横向、纵向滚动，适配系统安全区；设置和历史弹窗内部仍可滚动。
- 左上集中游戏菜单和暂停，菜单内提供设置、历史、重开、回首页；右上显示下一球和玩法提示。
- 底部固定「逆时针 / 发射 / 顺时针」三个按钮；点按转动 1°，长按连续转动，角度限制为 ±78°。
- 手机版禁止棋盘触摸瞄准和发射；取消设置中的独立发射选项，旧设置不影响新版操作。
- 包名和发行签名与 1.3.0 一致，可覆盖安装；versionCode 升至 10400。
- 验证：19 项自动测试，含移动端触摸禁用、旋转方向/边界、暂停和结算期间输入锁定；320×568、390×844 浏览器界面及操作检查。未进行实体手机安装实测。

下载：[1.4.0 APK](https://github.com/Nanami0923/Orbbound/releases/download/v1.4.0/Orbbound-1.4.0-Android.apk)。本地输出：`最终交付/Orbbound-1.4.0/Orbbound-1.4.0-Android.apk`。

## 1.3.0 安卓版

- 新增 Android 7.0 及以上可安装的离线 APK，包名 `com.orbbound.game`，versionCode `10300`。
- 手机界面压缩顶部信息区，按屏幕高度缩放棋盘，保留拖动瞄准、松手发射和独立发射按钮。
- 切后台自动暂停并保存；系统返回键依次关闭弹窗、返回首页、将应用放入后台。
- 本地成绩、设置和续局保存在应用内；卸载或清除应用数据会删除记录。
- 下载：[GitHub Release v1.3.0](https://github.com/Nanami0923/Orbbound/releases/tag/v1.3.0)。

本机构建需要 JDK 21、Android SDK platform 36 和 build-tools 36.0.0，设置 `JAVA_HOME` 和 `ANDROID_HOME` 后运行：

```powershell
npm ci
npm run package:android
```

输出：`最终交付/Orbbound-1.3.0/Orbbound-1.3.0-Android.apk`，附 SHA-256 校验文件。
首次打包会创建专用发行签名，保存于 `%LOCALAPPDATA%/Orbbound/signing`，密码通过 Windows DPAPI 加密。后续版本复用该签名以便覆盖安装；请保留该目录与当前 Windows 用户环境，迁移构建机前应安全备份和导出签名。签名资料不提交 GitHub。

验证：规则及生命周期测试、生产构建、手机尺寸浏览器检查和 APK 签名校验。尚未连接实体安卓手机进行安装实测。

## 1.2.0 修复

- 棋盘下降时翻转六角行偏移，渲染、邻接搜索和射线预测共享该偏移；保留既有小球位置关系。
- 下降动画结束前锁定下一次发射；换局时恢复暂停的动画管理器，修复漫游独立发射卡住。
- 界面改用“同色消除 / 悬空掉落”和剩余发射次数倒计时，难度显示简单、普通、困难及具体开局参数。
- 新规则标识为 classic-v2。旧版未完成对局不加载，保留原记录并提示重新开局；历史成绩和设置不变。
- 15 项测试通过，含 60 组开局、超过 1,000 次发射的回归覆盖。

基于《Snood 现代化重构方案》的原创桌面版 MVP。保留“匹配—断连—掉落—危险条”的策略核心，不使用原作名称、素材或声音。

## 已实现

- 经典三连消除、顶部连通检测、悬空掉落、计分、危险条与棋盘下移。
- 漫游 / 经典 / 风暴三档难度。
- 鼠标、触屏拖动、左右方向键、Space 发射、Esc 暂停。
- 一次反弹瞄准预览，预测与实际共用确定性轨迹算法。
- 教程、暂停、设置、减少动态效果、独立发射按钮。
- 本地最高分与完整回合续局；同种子和同输入序列可复现。
- TypeScript + Phaser + Vite，规则层位于 `src/core`，不依赖 Phaser。

## 本地开发

```powershell
npm install
npm run dev
```

规则测试：

```powershell
npm test
```

生产构建：

```powershell
npm run build
```

## Windows 10 EXE

当前 Windows 1.3.0 为 Windows 10 x64 目录版：

`最终交付/Orbbound-Windows-1.3.0/win-unpacked/Orbbound.exe`

双击即可运行，无需安装。它是未签名的本地构建，Windows SmartScreen 可能显示提示；确认文件来源可信后再运行即可。

重新打包：

```powershell
npm run package:win
```

打包目标为 x64 目录版，Electron 只负责承载 `dist` 静态资源，不需要服务器或联网。复制游戏时必须保留整个目录。

## 1.1.0 更新

- 棋盘按窗口高度完整居中，统计和操作分别放在左右侧。
- 关闭辅助线后，发射器显示 36 像素方向短线，棋盘内隐藏光标。
- 本地保存最近 500 局，完成对局的前 10 名独立保留；记录开始时间、用时、分数、难度、结果与发射次数。暂停不计时，重开记录不参与排名。
- 使用共享棋子纹理、60 FPS 上限、首页休眠和有界历史，减少重复资源及运行开销。
- 设置、存档及记录保存在当前 Windows 用户的 `%APPDATA%/orbbound` 中，与游戏目录分离；旧版没有记录过的历史无法补回。


## 1.7.0 高清小球与无阴影显示

- 移除小球外侧投影，球面和描边限制在自身边界内，避免相邻小球相互遮挡。
- 游戏画布由 640×760 提升至 1280×1520，保持棋盘比例和原有操作布局。
- 共享小球纹理由 96×96 提升至 192×192，游戏内文字同步提高绘制清晰度。
- 保留原有碰撞规则和发射手感，关闭像素取整以支持平滑位置显示。

版本 1.7.0，versionCode 10700；沿用原签名，可覆盖安装保留记录。
